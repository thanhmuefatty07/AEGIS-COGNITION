# AEGIS-COGNITION Python dependencies
# Compatibility export for environments that cannot consume `uv.lock` yet.
# Authoritative dependency intent: pyproject.toml; authoritative resolution: uv.lock.
blake3>=0.4,<2
fastapi>=0.110,<1
openai>=1.40,<2
playwright>=1.40,<2
pytest>=8,<9
pytest-asyncio>=0.23,<1
python-dotenv>=1,<2
pyyaml>=6,<7
ruff>=0.3,<1
uvicorn[standard]>=0.27,<1
