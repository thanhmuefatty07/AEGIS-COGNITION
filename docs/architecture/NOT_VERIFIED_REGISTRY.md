# NOT VERIFIED registry

The machine-readable source used by deployment gates is
[`not_verified_registry.json`](not_verified_registry.json); blocker policy is
[`deployment_policy.json`](deployment_policy.json). This Markdown table is a
human-readable companion and cannot clear a blocker by itself.

This registry is intentionally explicit. An unavailable platform or privilege
does not become a pass, and it does not get silently removed from the closure
scope. Each row has an owner and a reproducible closure procedure.

| ID | Area | Reason | Risk | Required environment / closure test | Owner | Status |
|---|---|---|---|---|---|---|
| NV-001 | Linux cgroup v2 live enforcement | Current Windows/local fixture is not privileged cgroup evidence | Process can escape limits if adapter wiring is wrong | Run `scripts/linux_cgroup_live_probe.py` on a privileged Linux runner: create isolated cgroup, apply CPU/memory/pids limits, attach child, verify live pressure, cancellation, deadline, and `cgroup.kill`; retain JSON/logs | Runtime | NOT VERIFIED |
| NV-002 | Windows Job Object live enforcement | Local probe proves assignment, containment, active-process limit, termination, and deadline cancellation, but allocation-pressure termination remains a separate scoped observation | Memory enforcement under pressure may differ from configuration/containment evidence | Run `scripts/windows_job_object_live_probe.py`; retain `artifacts/local-runtime/windows-job-object-live-<sha>.json`. The report distinguishes configured memory ceilings from an observed allocation-pressure kill. | Runtime | IMPLEMENTED / PARTIALLY LIVE VERIFIED |
| NV-003 | macOS controls | Adapter is cooperative/measurement-only | No kernel-equivalent limit is claimed | Run `scripts/macos_capability_probe.py` on macOS: report CPU/memory/process/thread/I/O/termination capability levels and run cooperative cancellation; do not claim kernel enforcement | Runtime | NOT VERIFIED |
| NV-004 | External signed attestation | GitHub attestation is unavailable for this private user-owned repository | Release provenance lacks provider signature | Public/eligible repository or supported attestation provider; rerun release and verify predicate subject digest; keep failure reason while unavailable | Release | NOT VERIFIED |
| NV-005 | H0/H1/H2 hardware policy freeze | Current local H0/H1/H2-labelled measurements retain p50/p95/p99, process metrics, queue, rejection, cancellation and fairness fields; labels are not representative hardware tiers and memory pressure remains unprobed | Resource defaults remain assumptions; local synthetic profiles cannot freeze cross-machine policy | Three named hardware profiles with raw JSON/CSV and markdown p50/p95/p99, RSS, CPU, queue, rejection, cancellation, fairness and pressure results; current local evidence is under `artifacts/verification/resource-policy-H*-20260911-r1.json` and remains local-only | Performance | MEASURED LOCAL ONLY / NOT VERIFIED FOR H0-H2 |
| NV-006 | External OpenTelemetry exporter | Internal bounded telemetry facade exists; exporter not exercised | Export correlation/failure semantics may drift | Configured OTLP endpoint: verify parent/child, retry, cancel, failure, finalization and dropped-event behavior without authority coupling | Observability | NOT VERIFIED |
| NV-007 | Full fuzz campaign | Current target/build/smoke does not prove all parser/protocol/runtime/FFI/archive targets | Hostile input regressions can remain undiscovered | Nightly >=10 minutes and weekly/release >=60 minutes where runner allows; retain seed, corpus, crashes, command, commit, duration and executions; zero reproducible crash/UB | Security | NOT VERIFIED |
| NV-008 | Tier-1 wheel parity | Linux/Windows attempts are required; macOS arm64 currently hits a hosted-runner rustup `cargo-fmt` component conflict before wheel build | Native packaging can diverge by platform | Linux/Windows/macOS builds for policy versions incl. 3.14, supported 3.14t, and 3.15 compatibility; clean install/import/native/runtime/metadata; repair or replace the macOS image conflict | Release | NOT VERIFIED |
| NV-009 | Rollback/restore drill | Local N -> N+1 -> N package rollback is retained; external persisted-fixture restore remains environment-specific | Release recovery may be untested outside the local package drill | Retained local drill: `artifacts/local-runtime/rollback-drill-<sha>.json`. Closure requires an isolated target with persisted fixture restore and replay-prefix verification. | Release | PROVEN LOCAL PACKAGE DRILL / RESTORE NOT VERIFIED |
| NV-010 | Branch protection | Provider API returned 403 for private repository rules | A bad commit may reach main without required checks | Repository owner enables required CI/deep/release checks and records ruleset URL/export; verify direct push and stale evidence are blocked | Repository | NOT VERIFIED |
| NV-011 | GT96 closure rows | Goal/Progress/Budget/Finalizer/Graph/Result/Evidence/Capability/Cache contract primitives are now implemented with direct Rust tests; full runtime integration and final-SHA manifest binding remain open | Architecture claims can outrun behavior | Execute each row in GT96_TRACEABILITY.md and update its status/evidence ID; rows not implemented stay explicit, not “complete” | Architecture | IN PROGRESS |
| NV-012 | Cross-version schema migration | Local fail-closed migration matrix and prefix recovery are retained; production-shaped expand/backfill/contract and external persisted-fixture rehearsal remain open | A persisted archive may be unreadable after upgrade | Retain local vN→current fixtures covering unknown optional/added/removed/truncated/corrupt/partial-tail/old/future schema; then run external expand/backfill/contract, rollback and persisted-fixture restore rehearsal | Replay | IMPLEMENTED / NOT VERIFIED |
| NV-013 | Miri/ASan closure | Only selected `resource::tests` are configured and final-SHA artifacts are not yet retained | Uncovered FFI/native/archive paths may differ | Run exact commands in `SANITIZER_SCOPE.md`, retain logs/toolchain/features/exclusions, and fail on reproducible UB | Security | NOT VERIFIED |
| NV-014 | Communication payload metrics | The dedicated matrix harness is now present; no universal zero-copy claim is made and platform-specific lanes remain unverified until retained | Copy/zero-copy claims may be overstated | Run `scripts/communication_payload_benchmark.py`; retain classes 64B, 256B, 1KiB, 4KiB, 16KiB, 64KiB, 256KiB, 1MiB, 16MiB with p50/p95, throughput, allocations, copies, RSS, CPU and unsupported-method reasons. | IPC | IMPLEMENTED / MEASURED LOCAL ONLY |
| NV-015 | GitHub hosted-runner evidence | Run `34266949093` at the recorded final SHA failed before runner allocation (zero steps/logs, 0 ms); the GitHub annotation explicitly reports an account billing/spending-limit condition | Remote multi-platform and release evidence cannot be independently retained until a runner-backed attempt is available | Resolve the repository-owner Actions billing/permission/service condition, rerun CI, AEGIS plugins, Deep evidence, and Release evidence on the final `main` SHA, then bind run IDs and artifacts in `current.json` | Repository / Release | NOT VERIFIED |
| NV-016 | Real multi-machine TCP cluster soak | Loopback or single-host tests do not establish network lease, recovery, and backpressure behavior across machines | Distributed coordination can fail under RTT, packet loss, or partial partition | Run `scripts/tcp_cluster_soak_gate.py` on multiple named hosts; retain RTT/loss, lease/recovery, queue pressure, cancellation and raw event-chain evidence bound to final SHA | Distributed Runtime | NOT VERIFIED |
| NV-017 | Full QuickJS interpreter cold-start | A lightweight JavaScript probe is not evidence for the complete interpreter artifact or semantics | Skill execution may diverge between the intended runtime and the fallback probe | Run `scripts/quickjs_cold_start_gate.py` with the pinned full QuickJS/Wasm artifact and semantic corpus; retain cold-start distribution, failures and artifact hash | Runtime Artifact | NOT VERIFIED |
| NV-018 | Live provider HTTP 429 soak | Mock or single-request fallback tests do not prove provider throttling behavior | Retry storms or route fallback can amplify spend and hide provider failure | Run `scripts/dynamic_provider_fallback_gate.py` against a real provider/account or an independently controlled HTTP 429 endpoint; retain retry, backoff, idempotency, route and budget evidence | Provider Reliability | NOT VERIFIED |
| NV-019 | External deployment smoke | Local import and loopback checks do not prove a clean external deployment | Packaging, configuration, network policy, or persistence can fail outside the checkout | Run `scripts/external_deployment_smoke_gate.py` in a clean container/target environment; retain image/package digest, startup, replay recovery, health and rollback evidence | Deployment | NOT VERIFIED |
| NV-020 | Trust-policy owner and cross-cell binding | Validated `AgentConfig` now creates the canonical subject; `Agent(lab=True)` binds it into the Lab mission, and the gateway receives it. Lab replay/event/cell binding is locally proven, but direct compatibility defaults still split (`DEV` vs `PROD`) and provider/browser/process/benchmark receipts are not cross-cell proven | A path could apply a different policy than the mission boundary or silently downgrade authority | Owner approves one default/precedence policy; run DEV/STAGING/PROD constructor matrix, verify the same subject hash at gateway, evidence, browser, process and benchmark boundaries, and retain native/fresh-process receipts bound to the final SHA | Architecture / Security | LOCAL LAB BINDING PROVEN / GLOBAL NOT VERIFIED |

The registry is retained after closure: a closed row changes status and adds
the closing evidence ID and SHA; it is not deleted.
